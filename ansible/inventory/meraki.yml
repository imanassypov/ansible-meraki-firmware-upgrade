---
# Meraki playbooks run entirely against the Meraki Dashboard API from localhost.
# No SSH connection to devices is required.
all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"
