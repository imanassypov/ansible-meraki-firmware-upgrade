# Terraform state — never commit
*.tfstate
*.tfstate.*
*.tfstate.backup

# Terraform working directory
.terraform/
.terraform.lock.hcl

# Merged data model generated by write_model_file — for inspection only
.merged_defaults.yaml

# Sensitive variable files
*.tfvars
*.tfvars.json

# Local env overrides
.envrc
